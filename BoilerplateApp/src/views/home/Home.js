import React, { useState, useContext } from 'react';
import {
    View,
    Text,
    StyleSheet,
} from 'react-native';

function Home() {
    return (
        <View style={styles.container} />
    );
}

const styles = {
    container: {
        flex: 1
    }
};

export default Home;